services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: nextdata
      POSTGRES_PASSWORD: nextdata
      POSTGRES_DB: nextdata
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
