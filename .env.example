DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
GROQ_API_KEY=gsk_...          # free at console.groq.com — primary key
GROQ_API_KEY_2=gsk_...        # optional second key for rate-limit failover
GOOGLE_API_KEY=AIza...        # free at aistudio.google.com — also powers the Gemini LLM fallback below
AGENT_MODEL=llama-3.3-70b-versatile
# GEMINI_FALLBACK_MODEL=gemini-2.5-flash  # used only if every Groq key is rate-limited
# ASK_TIMEOUT_SECONDS=60      # optional: request timeout override
